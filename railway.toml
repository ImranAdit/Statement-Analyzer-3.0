[build]
builder = "dockerfile"

[deploy]
startCommand = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/calculate"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
